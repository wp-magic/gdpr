{% include 'magic-form-checkbox.twig' with {
  name: 'allow_cookies',
  before: post.before_allow_cookies_text,
  after: post.after_allow_cookies_text,
  error: 'cookie',
  error_msg: post.error_cookie,
} %}
